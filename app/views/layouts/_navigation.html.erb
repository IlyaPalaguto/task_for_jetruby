<%= link_to 'Admin panel', admin_root_path if current_user&.admin? %>
<%= image_tag current_user.avatar_url(:small), class: 'avatar' if user_signed_in? %>
<%= link_to 'log out', destroy_user_session_path, method: :delete %>
